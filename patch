local path ="rostruct/cache/releases/saucekid-saucevr-LATEST-ZIPBALL/src/Util/FixChatted.lua"
local yourstupidgaypatch = [[
local Players = game:GetService("Players")
local TextChatService = game:GetService("TextChatService")
local LocalPlayer = Players.LocalPlayer
TextChatService.MessageReceived:Connect(function(message)
    local textSource = message.TextSource
    if not textSource then
        return
    end
    local player = Players:GetPlayerByUserId(textSource.UserId)
    if not player or player == LocalPlayer then
        return
    end
    firesignal(player.Chatted, message.Text)
    firesignal(Players.PlayerChatted, nil, player, message.Text, nil)
end)
return true
]]
writefile(path, yourstupidgaypatch)
if getgenv().debugfornerds then
    print(readfile(path))
    warn("if it didnt print the code it didnt work")
    print("also you're a fucking loser for toggling debugfornerds you actual dork. you'll NEVER amount to ANYTHING")
end
